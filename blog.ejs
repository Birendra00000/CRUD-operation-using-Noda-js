<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Page with Navbar and Card</title>
  </head>
  <body>
    <!-- Navbar with inline CSS -->
    <%- include("components/navbar") %>

    <!-- Card with inline CSS -->

    <% blogs.length > 0 && blogs.map((blog)=>{ %>
    <div
      style="
        margin: 20px;
        padding: 20px;
        border: 1px solid #ddd;
        border-radius: 5px;
        box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2);
        max-width: 300px;
      "
    >
      <h2 style="color: #333; font-size: 24px"><%- blog.title%></h2>
      <!-- DYNAMIC CONTENT DEKHAUNA PARYO VANYA MINUS HUNXA EJS VITRA -->
      <h3 style="color: #666; font-size: 18px"><%- blog.subTitle%></h3>
      <p style="color: #888"><%- blog.description%>.</p>
      <a href="/single/<%- blog.id %>">Read more...</a>
    </div>
    <% }) %>
  </body>
</html>
